import { Loader.Interceptor } from './loader.interceptor';

describe('Loader.Interceptor', () => {
  it('should create an instance', () => {
    expect(new Loader.Interceptor()).toBeTruthy();
  });
});
